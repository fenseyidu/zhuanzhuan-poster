# 更新说明

## 会员日头图能力

- 新增 `会员` 业务线，以及会员日 2:1 头图的专用模板、参考素材和合成脚本。
- 扩展参数库与业务规则，支持会员日商品槽位映射、下方商品组构图和固定图层合成。
- 增加会员日 A/S 档位的联动版式预检：标题与下方商品组必须同时通过检查后，才进入固定图层合成。

## 本次规则调整

- 将毛笔主标题的可见笔墨视觉中心固定为画面高度的 45%。
- 提示词不再向图像模型传递标题框坐标范围，避免模型将坐标区间误解为构图指令。
- 联动校正仅使用“标题 45% 中心 + 下方商品区”作为共同锚点，保持标题与商品组整体校正。

## 本次更新：标题独立生成与合成

- 会员日 A/S 2:1 头图调整为两路生成：商品底图不再生成主标题，主标题以透明 PNG 单独生成后再合成。
- 新增 MasterGo 导出的无标题底图和毛笔标题风格参考图，作为商品底图与标题专用图的默认参考。
- 合成器按标题 PNG 的可见 alpha 内容裁切、等比缩放并居中，避免将整张标题画布强制拉伸造成笔画变形。
- 标题与商品底图可独立进行 QA 和重试；最终合成仍会检查标题、商品与固定图层的碰撞。

## 本次版式微调

- 会员日标题层上移至 `Y=350`，可见标题高度调整为 `280`，以改善标题与品牌、日期固定层之间的留白关系。
- 模板、技能说明与构图规则已同步采用这组标题层参数。

## 本次更新：会员 B 与本地文字排版

- 新增会员 B 2:1 的本地标题与副标题排版流程，并在标题上方按标题左边界放置会员标识。
- 会员 A/S 主标题默认使用思源宋体 SC Heavy；仅在补充要求包含 `特殊字` 时，整行切换为内置毛笔标题参考字形。
- 新增标题宽度上限、语义换行和副标题字号/间距配置，保证不同画布尺寸下的文字排版一致。
- 会员 A/S 增加礼盒与普通商品两类前景分支，并由渲染器处理非 2:1 底图的裁切适配。
- 会员图片生成不再自动无限重试；视觉 QA 失败后先征询用户是否继续定向重生。生成结果与复盘文件统一归档到 `~/Documents/转转海报输出/`。

## 本次更新：标题适配与重试策略

- 会员 A/S 主标题固定为单行；超出最大宽度时，标题资产渲染器按比例缩小字号适配，不再自动折行。
- 合成阶段以标题可见高度和最大可见宽度为双上限等比缩放，避免长标题溢出或变形。
- 会员 B 2:1 补充左侧文字阅读区、右侧主视觉区的版式约束。
- 会员图片视觉 QA 未通过时，自动执行至多两次单目标定向重生；第二次仍失败则明确报告阻塞原因并停止。

## 本次更新：会员日底图前景比例

- 会员日 AI 底图明确为上方约 68% 的干净暖金文字阅读区，以及下方约 32% 的贴底前景区。
- 商品、低矮打开的礼盒、金币与丝带在前景区内紧凑层叠，避免侵占标题和固定图层的阅读空间。

## 本次更新：特殊字标题与会员 B 间距

- 当会员 A/S 的补充要求包含 `特殊字` 时，标题按可见 alpha 内容固定为 268px 高并保持比例，不再受默认标题宽度上限限制。
- 默认会员 A/S 标题仍使用单行缩放适配规则；特殊字标题通过独立合成参数处理。
- 会员 B 2:1 的标题与副标题间距调整为 64px，改善标题组的上下留白。

## 本次更新：会员 B 标题组居中

- 会员标识、主标题与非空副标题改为作为一个整体垂直居中；主标题换行时重新计算整组高度。
- 在 2250px 宽成图中，标识与标题、标题与副标题的可见间距统一为 62px。
- 会员 B 的长中文标题优先按空格或标点换行，无法按语义切分时才按字符边界换行。

## 本次更新：会员 S 节点化头图

- 新增会员 S 2:1 路线，支持 `会员节点` 输入（圣诞 / 中秋）；未填写时可从主标题或补充要求中的明确节点词识别。
- 圣诞节点新增独立的 MasterGo 无标题背景母版、档位配置和本地 `membership_s_renderer.py`，不复用会员 A 的商品槽位、标题资产或固定层。
- 会员 S 以节点决定背景路线：无商品时使用节日礼盒组合；有 1–4 件商品时按公共商品关系选择组合。
- 主标题保持单行并按宽度缩小，副标题可折行；标识、标题和副标题整体垂直居中，并根据背景自适应共享文字颜色。

## 本次更新：会员 S 圣诞光场融合

- 圣诞节点的商品或礼赠组必须嵌入光树下部体积光，继承同一暖金主光、地面反射与柔和接触阴影，避免出现独立棚拍式商品。
- 新增圣诞商品光场融合的视觉 QA 与定向修正规则：商品过大、位于光束前方、边缘生硬或单独布光时均视为底图失败。
- 圣诞背景调整为更通透的低饱和松绿中间调，保留左侧连续阅读区和右侧暖金主题焦点。
- 会员 S 选定节点若没有注册图生图参考母版，流程会先要求补充该节点的参考图。
